import React, { useContext } from 'react';
import { View, Text } from 'react-native';

export default CartScreen = ({ navigation }: any) => {

return (
    <View>
        <Text> Carrinho de compras</Text>
    </View>
);
};